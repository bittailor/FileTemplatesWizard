${string.name}

${string.x}

${string.y}

   ${string.name}
     ${string.name}


   ${string.x}
 ${string.name}
 
 ${string.name?upper_case}
 
<#if boolean.save>
	boolean save is true
</#if>

<#if !boolean.save>
	boolean save is not true
</#if>

